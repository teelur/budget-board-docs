# OIDC Authentication

If you plan to use an OIDC provider for authentication instead of the built-in username/password system, you will need to configure the following settings in your `compose.env` file.
